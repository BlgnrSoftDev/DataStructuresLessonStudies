#include <stdio.h>
#include <stdlib.h>
#include "StackADT.h"

int main()
{
    STACK* S = createStack();
    void* data;
    while(S->count != 0)
    {
        data = popStack(stack);


    }


    return 0;
}
